#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
*main - A program to print lower case alphabet.
*Return: 0 (success)
*/
int main(void)
{
char = A;
for (A = 'a'; A = 'z'; A++)
{
putchar(A);
putchar('\n');
}
return (0);
}
